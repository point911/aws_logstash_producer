---

logstash:

  inputs:
    file:
      path: /tmp/output.log

  outputs:
    kafka:
      host: "{{ hostvars[groups['tag_Name_learnkafka'][0]]['ec2_private_ip_address'] }}:9092"
      topic_id: logstash

  plugins: []

