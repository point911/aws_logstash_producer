---

logstash:

  inputs:
    file:
      path: /tmp/output.log

  outputs:
    kafka:
      bootstrap_servers: "{{ groups['security_group_kafka']|map('extract', hostvars, 'ec2_private_ip_address')|list|join(':9092,')+':9092' }}"
      topic_id: logstash

  plugins: []

