---

logstash:

  inputs:
    file:
      path: /tmp/output.log

  outputs:
    kafka:
      bootstrap_servers: "{{ hostvars[groups['tag_Name_kafka1'][0]]['ec2_private_ip_address'] }}:9092,{{ hostvars[groups['tag_Name_kafka2'][0]]['ec2_private_ip_address'] }}:9092,{{ hostvars[groups['tag_Name_kafka3'][0]]['ec2_private_ip_address'] }}:9092"
      topic_id: logstash

  plugins: []

